# SDDM autologin — logs `admin` straight into the Plasma X11 session.
# Using plasmax11 (not plasma/Wayland) for maximum driver compatibility on
# live boot; users can switch to the Wayland session from the SDDM chooser.
[Autologin]
User=admin
Session=plasmax11.desktop
Relogin=false
