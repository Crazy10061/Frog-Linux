displaymanagers:
  - sddm

# executable is the session binary, not a .desktop name. desktopFile is the
# basename Calamares writes into the display manager's session setting.
defaultDesktopEnvironment:
  executable: "startplasma-wayland"
  desktopFile: "plasma"

sysconfigSetup: false

basicSetup: true
